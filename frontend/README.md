# EduBridge Portal — Frontend

This is a Vite + React scaffold for the EduBridge Portal frontend.

Quick start:

```bash
cd frontend
npm install
npm run start
```

Set `VITE_API_URL` in a `.env` file to point to your backend (e.g., `http://localhost:4000/api`).
